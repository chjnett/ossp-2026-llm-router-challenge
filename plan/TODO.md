<!--
SPDX-FileCopyrightText: Copyright 2026 chjnett
SPDX-License-Identifier: Apache-2.0
-->

# TODO — Efficient LLM Routing Challenge

1인 풀타임 · 2026-08-10 시작 · **마감 2026-08-27 18:00 KST**
규칙 [RULES.md](RULES.md) · 설계 [DESIGN.md](DESIGN.md)

각 Phase 끝에 **게이트**가 있다. 게이트를 통과 못 하면 다음 Phase로 넘어가지 않는다.

---

## 현재 상태 (2026-08-16 갱신) — 이 절이 최신 작업판이다

제출 챔피언 **`t30-headroom-b1075-p100`**.

| | |
| --- | ---: |
| Dev 가중 (호스트 출력 → 공식 채점기) | **0.679290** |
| 등급별 | fast 0.652273 · balanced 0.684659 · premium 0.709943 |
| 예산 사용 | 1.1105/1.25 · 1.5619/2 · 2.6179/4 |
| Train 5-fold OOF | **0.663452** |
| Train 4/6/8-fold OOF | **0.662429 / 0.661832 / 0.661662** |
| 파산 게이트 | 최종 재적합 기본 0/36,000 + 추가 seed 0/36,000 |
| LOFO | **9/9 family × 세 tier 예산 통과**, 결합 0.652202 |
| nested 판정 | T30 미실행; T17/T19는 소표본 inner 0점 |
| 최종 경계 | 초기 q80 피봇 3/36,000 → tier별 size penalty로 **0/36,000** |
| 테스트 | 전체 396개 중 381 pass / 14 skip / 기존 Python 3.14 메시지 불일치 1 |
| 이미지 | 압축 63.4 MiB / rootfs 195.8 MiB (한도 1024 / 2048) |
| 실행 | 2,640문항 최장 8.116초 (한도 90초) |
| 아티팩트 | 223.5 KB · SHA-256 `8b0087c9…9f55a1` |

### 8/14 실험 판정

- [x] T0: hash-regex와 선택 차이 1,179건 분석. 손실 상위는 `sym_math`
      +0.018239, `code_io` +0.011534
- [x] T1: 두 계열에만 조건부 이득 혼합 적용 → OOF 0.633523, 안전 통과
- [x] T2: early/late 증분 반응 헤드 → OOF 0.6314~0.6320, **폐기**
- [x] T3: digits/identifier/shape 템플릿 → OOF 0.6311~0.6324, **단독 폐기**
- [x] T4: 5% 블렌드 단일 OOF 0.633750 → nested OOF −0.000227, **승격 취소**
- [x] T5: 270차원 signed hash ridge → OOF 0.6539 / Dev 0.6882,
      raw 비용 후보 578/3,600 초과, 안전형 11/36,000 초과로 **미승격**
- [x] T6: 계열별 총합 calibration → OOF 최대 0.6553, Fast heavy-tail로 **미승격**
- [x] T7: 정규 확률제약 → OOF 예산 초과, heavy-tail 가정 불일치로 **폐기**
- [x] T8~T10: 보수 비용헤드 결합·tier별 mu·runaway/K1 gate → 안전/점수 동시 실패
- [x] T11: 문항별 상대비용 tail-risk pricing → OOF 0.636818,
      최종 8/36,000 초과. 0회 후보는 OOF 0.632074라 **미승격**
- [x] T12: tier별 점수 헤드. 최종 후보 OOF 0.633750이지만
      mild shift Fast 1/36,000 초과로 **미승격**
- [x] T13: 초대형 수치·깊은 LaTeX tail guard. sp0 15회 / sp2 6회 초과로 **미승격**
- [x] T14: code_io 조건부 단일노출 cap 추가 → OOF 0.638409 / Dev 0.669574,
      **0/36,000회로 챔피언 승격**
- [x] T14 복수 fold 재검증 → 4/6/8-fold 모두 T1보다
      **+0.002642 / +0.004744 / +0.003281**
- [x] T14 아티팩트 재생성
- [x] T14 arm64 전체 제출 스택·ID/순서 감사·공식 채점 통과
- [x] T15 score alpha 32,000 → OOF 0.654673 / Dev 0.670426
- [x] 일반화 자동화: 4×3 nested + leave-one-family-out 평가 도구 추가
- [x] T15 LOFO 9/9 family 전 tier 통과, 결합 OOF 0.637912
- [x] T15 최종 0/36,000, arm64 제출 스택·공식 채점·런타임 통과
- [x] T16 tier별 alpha → 5-fold만 상승, 4/6/8-fold·Dev 하락으로 **폐기**
- [x] T17 내부 OOF 비용 tail q80 → OOF 0.658509 / Dev 0.676648
- [x] T17 4/5/6/8-fold 모두 T15 초과, LOFO 9/9 전 tier 통과
- [x] T17 경계 실패 3/36,000을 tier별 size penalty로 0/36,000으로 피봇
- [x] T17 arm64 전체 제출 스택·ID/순서 감사·공식 채점·런타임 통과
- [x] T18 기존 보조 헤드 조건부 혼합 → fold 혼재/Dev 하락으로 **폐기**
- [x] T19 Premium 취약 family local ridge → 4/5/6/8-fold·Dev·LOFO 동시 상승
- [x] T19 0/36,000 및 linux/arm64 전체 제출 스택 통과로 챔피언 승격
- [x] Train OOF same-cost oracle 도구 추가 → T19 regret 0.108608, T22 0.106307 측정
- [x] T20 hashed response → fold/Dev 재현 실패로 **폐기**
- [x] T21 same-family kNN → Balanced 신호는 확인했으나 안전 보정 후 이득이 작아 **미승격**
- [x] T22 Balanced 상대비용 cap 10 → 4/5/6-fold 상승·8-fold 동률·Dev 상승
- [x] T22 기본 0/36,000 및 LOFO 9/9 통과로 챔피언 승격
- [x] 최종 Train+Dev 재적합으로 추가 seed Fast 반례 제거
- [x] T26 Balanced `code_io` cap 3.5로 seed3027 반례 제거, 기본·추가 합계 0/54,000
- [x] 실제 LightGBM score OOF 실험 → dense/hash 모두 일반화 이득 없어 **폐기**
- [x] T28 headroom 1.1/1.1 → fold 상승, extreme shift 1/2,000으로 **폐기**
- [x] T29 기본·확장 0/72,000이나 최대 0.995/0.992로 경계 노출 확인
- [x] T30 headroom 1.075/1.0 → T26 대비 복수 fold 상승, 최대 0.986/0.978

### 지금 할 일 — 공격 연구와 제출 안전선 분리

- [x] T1 고정 상태에서 hash 표현·비용 보정·tier 경계 스윕
- [x] OOF 점수와 6개 분포 이동 파산률을 `results.jsonl`에 기록
- [x] `mu`·`size_penalty`의 tier별 정책 지원
- [x] 비용 tail 통계를 in-fold 적합이 아니라 내부 OOF 잔차로 추정
- [x] Fast의 ax31 폭주를 계열 평균이 아닌 조건부 위험으로 모델링
- [x] T15가 4/5/6/8-fold 안정성 통과 (`folds`를 실험 로그에 명시)
- [x] T17이 복수 fold 안정성 통과; nested 소표본 실패는 명시적으로 유지
- [x] leave-one-family-out 미지 계열 폴백 평가
- [x] OOF 자기행 누수·입력 순서 불변성·artifact 왕복 전용 테스트 추가
- [x] T19 아티팩트와 챔피언 ID 일치 및 Docker 제출 스택 재검증
- [x] T22 아티팩트와 챔피언 ID 일치 및 linux/arm64 Docker 제출 스택 재검증
- [x] T26 아티팩트와 챔피언 ID 일치 및 linux/arm64 Docker 제출 스택 재검증
- [x] T30 아티팩트와 챔피언 ID 일치 및 linux/arm64 Docker 제출 스택 재검증

### 제출 경화 상태

- [x] 제출 절차 리허설 (로컬 레지스트리로 push·측정까지) → [RELEASE.md](RELEASE.md)
- [x] 공식 크기 측정 (`router-measure-image`)
- [x] SBOM 생성 → `container/sbom.cdx.json`
- [x] 결과보고서 원고 → [REPORT.md](REPORT.md)
- [x] 커밋↔이미지 결속 관문 (`tools/verify_release.py`) + CI 연결
      — `image-binding` 작업이 arm64 러너에서 초록. 실제 검사 수행 확인
- [ ] **사용자 몫**: 공개 레지스트리 계정, 데모 영상, 공식 양식 다운로드
- [ ] 8/25 이미지 동결
- [ ] P5 제출 절차 실행

> 아래 P0~P5 체크리스트는 최초 일정과 완료 근거를 보존한 역사 기록이다.
> 현재 실행 우선순위는 위의 “지금 할 일”을 따른다.

| Phase | 기간 | 시간 | 산출물 |
| --- | --- | ---: | --- |
| P0 기반 | 8/10–8/11 | 14h | 오프라인 하네스 + 파산 게이트 |
| P1 최소 동작 라우터 | 8/12–8/14 | 21h | baseline을 이기는 end-to-end 라우터 |
| P2 예측기 강화 | 8/15–8/19 | 35h | 점수·비용 헤드 (8/17 하드 체크포인트) |
| P3 예산 안전 공학 | 8/20–8/22 | 21h | 파산 0% 구성 확정 |
| P4 컨테이너·제출물 | 8/23–8/25 | 21h | arm64 이미지 + 라이선스 + 보고서 |
| P5 버퍼·제출 | 8/26–8/27 | — | 제출 |

---

## P0 — 기반 (8/10–8/11, 14h)

> 목표: **모든 후속 실험을 자동으로 판정하는 장치**를 먼저 만든다.
> 이걸 나중에 만들면 P2 결과를 신뢰할 수 없다.

### 저장소
- [ ] `chjnett/ossp-2026-llm-router-challenge` fork → clone
- [ ] `plan/` 3문서 커밋 (RULES / DESIGN / TODO)
- [ ] `python3 -m unittest discover -s tests -p 'test_*.py'` 통과 확인
- [ ] `tools/materialize_public_data.py`로 Train 1,760 / Dev 880 생성, SHA 대조
- [ ] 네 baseline 재현해 `baselines/README.md`의 Dev 수치와 일치 확인

### 오프라인 하네스 — 가장 중요한 산출물
- [ ] `src/ossp_router/scoring.py`를 그대로 호출하는 평가 함수 작성
      (자체 구현 금지 — 공식 `Decimal` 경로를 써야 한다)
- [ ] Train 5-fold CV 스플릿 고정 (콘텐츠 해시 기반, 재현 가능)
- [ ] 실험 기록을 append-only JSONL로 남기는 러너
      — 등급별 (점수, **실제 비용 비율**, 한도 대비 여유), 설정 스냅샷, 커밋 SHA
- [ ] 한 줄로 "구성 A vs B" 비교 리포트 출력

### 실험 인프라 — 유연성의 실체 (DESIGN §5-B)
> 이걸 P0에 만들어야 P2에서 하루 수백 개 설정을 돌릴 수 있다. 나중에 만들면 못 만든다.
- [ ] 파이프라인 6단계를 프로토콜로 고정 (`[F] [Q] [C] [G] [A] [S]`)
- [ ] 구현 레지스트리 — 이름으로 구현을 꽂는 구조 (`"Q": ["knn@v2", "linear@v1"]`)
- [ ] `run.py --config exp-NNN.json` → `results.jsonl` 한 줄 추가
- [ ] 3층 캐시: 특징(추출기 버전+입력 해시) / 트랙별 out-of-fold 예측 / 할당 실험은 무캐시
- [ ] **캐시된 배열 위에서 할당·게이트 실험이 1초 미만**인지 확인 ← 이 게이트가 핵심
- [ ] `champion.json` — 파산 게이트 통과 구성 중 CV 최고. 자동 승격 로직
- [ ] 순위표 출력 (`results.jsonl` → 정렬된 표)

### 파산 게이트 (RULES C4)
- [ ] 분포이동 시뮬레이터: Dirichlet 계열 재가중 (conc=5 / conc=0.7)
- [ ] leave-one-family-out 시나리오 (계열 하나를 학습에서 빼고 그 계열이 지배하는 배치로 검증)
- [ ] 배치 크기 변동 (200 / 800 / 2,640)
- [ ] **N ≥ 2,000회**, 3등급 초과 횟수 리포트
- [ ] 게이트를 CI에서 실행 가능하게 (실패 시 non-zero exit)

### 불변성 property 테스트 (RULES B)
- [ ] ID 셔플 + 순서 셔플 후 선택이 **비트 단위 동일**한지 검사
- [ ] 같은 프롬프트 중복 삽입 시 같은 선택인지 검사
- [ ] 같은 입력 2회 실행 시 출력 바이트 동일한지 검사
- [ ] 공개 Train/Dev 전체에서 통과

**게이트 P0** → 하네스가 `always_light`와 `hash_regex`를 정확히 재현하고, 파산 게이트와 불변성 테스트가 동작한다.

---

## P1 — 최소 동작 라우터 (8/12–8/14, 21h)

> 목표: **8/14에 이미 제출 가능한 것**이 있어야 한다. 이후는 전부 개선이다.

### 특징 추출 [F]
- [ ] `extract(text: str) -> Features` — tier도 ID도 받지 않는 시그니처
- [ ] 길이·언어·코드·수식·숫자 밀도·메시지 구조 (baseline 특징 재사용)
- [ ] 계열 판별 신호: MCQ 보기 패턴, `def f(`+`assert`, 논리 서술 어휘, 장문 여부, 한글 비율
- [ ] 입력토큰 추정: 문자 수 → 토큰 수 선형 회귀 (모델 간 상관 0.9998이므로 하나면 충분)

### 게이트 [G] — **최우선. 예산의 절반이 여기서 나온다**
- [ ] 동점 탐지기: 세 모델 동점 확률 예측 → 높으면 light 고정 (Dev 49%가 대상)
- [ ] 무망 탐지기: 셋 다 0점 확률 예측 → light 고정 (Dev 9%)
- [ ] K1 금지 규칙: 장문 문맥·논리 추론 계열, 예측 출력토큰 상위 분위
- [ ] 탐지기 정밀도·재현율과 **절약된 예산량**을 함께 기록

### 비용 헤드 [C] v1
- [ ] 계열 조건부 출력토큰 예측 (계열 중앙값에서 시작)
- [ ] 분위 회귀 τ=0.75로 상방 편향
- [ ] 로그공간 예측 시 Jensen 보정
- [ ] 예측 vs 실제 비용 비율 산점도로 편향 방향 확인

### 점수 헤드 [Q] v1 — T3 kNN
- [ ] 템플릿 정규화(숫자→`#`, 공백 정규화) 후 근접 이웃 검색
- [ ] 이웃의 outcome 통계로 `ŝ_m` 예측, 이웃 수 부족 시 계열 평균으로 축소
- [ ] 색인을 JSON/바이너리로 export (런타임에 로드만)

### 할당기 [A]
- [ ] λ 이분탐색 (`math.fsum` 집계, 콘텐츠 해시 정렬, 결정적 동률 처리)
- [ ] 분산 페널티 μ 자리만 만들어 두기 (값은 P3에서)
- [ ] 안전 검증 [S]: 비관 비용으로 재검사 후 저ROI부터 강등

### 런타임 골격
- [ ] `router-run` 인터페이스 구현 (`--input --tier --output`)
- [ ] 시작 직후 all-light 결정 확보 → 전역 예외 흡수 → 폴백
- [ ] 54초 워치독
- [ ] 원자적 출력 (`os.replace`, `0644`)

**게이트 P1** → Dev 가중 **≥ 0.68**, 3등급 예산 통과, 불변성 테스트 통과, 파산 게이트 통과.
(참고: hash-regex 0.6954, all-light 0.6193)

---

## P2 — 예측기 강화 (8/15–8/19, 35h)

> 목표: 점수 헤드 개선. **§2.5에서 점수 예측이 비용 예측보다 4배 값짐이 확인됐다.**
> 단 §6에서 "R²를 올릴 방법"은 아직 미검증 가설이다.
>
> **운영 방식은 순차 계획이 아니라 실험 큐다.** 아래 항목은 실행 순서가 아니라
> **우선순위가 붙은 대기열**이고, 매일 순위표를 보고 다음에 뭘 할지 고른다.
> 챔피언은 항상 제출 가능하므로 어떤 실험이 망해도 손실이 없다 (DESIGN §5-B).

### 매일 반복하는 루프
- [ ] 순위표 확인 → 챔피언 대비 개선 여지가 큰 항목을 대기열에서 꺼냄
- [ ] 설정 파일 하나로 실험 → `results.jsonl` 기록
- [ ] 두 조건(CV 가중 초과 + 파산 게이트 통과) 만족 시 챔피언 자동 승격
- [ ] 하루 끝에: 이틀 연속 챔피언을 못 넘긴 트랙은 대기열 뒤로 미룸

### T3 kNN 심화 (8/15–8/16)
- [ ] 정규화 스킴 실험: 숫자만 / 식별자까지 / 코드 AST 골격
- [ ] 거리 함수: 자카드 vs 코사인 vs 편집거리
- [ ] k와 축소 강도를 Train CV로 튜닝
- [ ] 이웃 없음 케이스의 폴백 경로 확인 (비공개셋에서 자주 발생할 수 있음)

### T5 계층적 축소추정 (8/16–8/17)
- [ ] 계열 → 하위군 → 문항 계층 정의
- [ ] empirical-Bayes 축소 (노이즈 sd 0.12~0.15를 명시적으로 모델링)
- [ ] 표본 수가 적은 군에서 상위 계층으로 얼마나 당길지 학습

### 🚦 8/17 24:00 — 방향 재점검 (중단이 아니라 재배분)
> 챔피언이 항상 있으므로 "전부 중단"할 필요가 없다. 남은 시간을 어디에 쓸지만 정한다.
- [ ] 순위표에서 챔피언의 CV 가중 최종을 확인
  - **≥ 0.705** → 대기열대로 계속. T2 착수
  - **0.69 ~ 0.705** → 새 트랙(T2) 착수 중단, **기존 트랙 튜닝 + 게이트 임계 탐색**에 집중
        (캐시 덕에 이쪽이 시간당 실험 수가 훨씬 많다)
  - **< 0.69** → 점수 헤드 투자를 접고 **P3 안전 공학을 8/18에 조기 시작**.
        폴백 사다리 2단계(kNN + 게이트 + λ)를 확실히 굳히는 쪽으로 전환
- [ ] 판정은 CV 가중 최종으로만 한다. R²는 진단 보조일 뿐 판정 기준이 아니다 (RULES D1)
- [ ] 어느 경로든 **8/19 24:00 이후 새 트랙 착수 금지**는 그대로 적용

### T2 GBDT (8/18–8/19, 재점검에서 계속으로 판정된 경우)
- [ ] 오프라인 LightGBM 학습 (점수 3헤드 + 출력토큰 3헤드 + 분위 헤드)
- [ ] 트리를 JSON export → numpy 벡터화 추론기 작성
- [ ] 추론 결과가 학습 프레임워크와 일치하는지 대조 테스트
- [ ] 2,640문항 실행 시간 측정 (30초 게이트)

### 블렌드
- [ ] Train out-of-fold 예측 위에서 T1/T2/T3/T5 가중치 학습
- [ ] 후보 선택·블렌드 가중치 학습에는 **Dev를 쓰지 않는다** (RULES D2)
- [ ] 챔피언 동결 뒤 제출 아티팩트만 공개 Train+Dev 전체로 최종 재적합하고,
      고정된 안전 게이트의 통과/실패 판정과 런타임 검증만 다시 수행
- [ ] 트랙별 킬 기준 적용 — 개선 없는 트랙은 버린다

**게이트 P2** → Dev 가중이 P1 대비 개선, 3등급 예산 통과, 파산 게이트 재통과
(RULES C5 — 예측기가 바뀌었으므로 안전 보정 자동 재실행)

---

## P3 — 예산 안전 공학 (8/20–8/22, 21h)

> 목표: **파산 0%**를 만든다. 여기가 hash-regex가 죽은 지점이다.
> 순서는 위험 순 **Premium → Balanced → Fast**.

### 비용 헤드 정밀화
- [ ] 분위 τ 스윕 (0.6 / 0.7 / 0.75 / 0.8 / 0.85) — 파산률과 점수를 함께 기록
- [ ] `sd(ôut_m)` 예측 헤드 추가 → 할당기의 분산 페널티 μ 활성화
- [ ] 조건부 상방 분위 보정: "예측 출력토큰이 X 이하일 때 실제 95분위는 얼마인가"를
      Train 교차적합으로 측정해 **문항별 상한**으로 사용 (conformal 방식)

### K1 하드 캡 (R1 핵심 대응)
- [ ] 예측 출력토큰 상위 분위 컷 스윕 — 컷과 파산률·점수의 트레이드오프 표
- [ ] 계열 기반 K1 금지 목록 확정 (BABILong, RuleTaker, HRMCR)
- [ ] 캡 적용 전후 Premium 파산률 비교

### 등급별 정책 확정
- [ ] Premium: 목표 사용률 스윕 → 파산 0% 지점 탐색
- [ ] Balanced: 동일
- [ ] Fast: 동일 (가중치 0.4로 가장 크므로 절대 잃으면 안 됨)
- [ ] **파산 0%가 처음 관측된 설정에서 util을 한 단계(−0.03) 더 내려 배포값으로 확정** (RULES C4)

### 강건성 검증
- [ ] N ≥ 2,000회 스트레스, 3시나리오 전부 0회
- [ ] leave-one-family-out 통과
- [ ] 배치 크기 200 / 800 / 2,640 전부 통과
- [ ] 작은 배치에서 λ 탐색이 불안정하지 않은지 확인

### 런타임 의존성 결정 (DESIGN §9-1)
- [ ] numpy 단독 vs 순수 stdlib — 재현성·라이선스 단순성 기준으로 확정
- [ ] 결정 근거를 DESIGN.md에 기록

**게이트 P3** → 3시나리오 × N≥2,000에서 **파산 0회**, Dev 가중이 all-light(0.6193) 대비 명확히 상회.

---

## P4 — 컨테이너·라이선스·제출물 (8/23–8/25, 21h)

### 컨테이너 (8/23)
- [ ] `container/Dockerfile` 수정, `linux/arm64` 빌드
- [ ] `docker run --network none --read-only --cpus 2 --memory 2g --pids-limit 32`로 실행 확인
- [ ] `tools/check_runtime.py`로 공개 Train/Dev 전체 3등급 실행 — **2,640문항 30초 이내**
- [ ] `OSSP_RUN_CONTAINER_TESTS=1 python3 -m unittest tests.test_runtime` 통과
- [ ] `VOLUME` 선언 없음 확인, 이미지 크기 측정 (압축 계층 1 GiB / rootfs 2 GiB 이하)
- [ ] stdout·stderr 출력량 확인 (각 1 MiB 미만)

### 라이선스·SBOM (8/24)
- [ ] 이미지에 포함한 모든 파일의 출처·버전·라이선스·SHA-256 기록
- [ ] 학습 산출물(색인·계수·트리)의 생성 절차와 입력 해시 기록
- [ ] 새 원본 파일에 SPDX 헤더 부착
- [ ] `ruff check .` / `reuse lint` 통과
- [ ] 기반 이미지 SBOM·취약점 재검사 (`container/BASE_IMAGE.md` 요구사항)
- [ ] 주 라이선스 `Apache-2.0` 유지 확인

### 문서·보고서 (8/25)
- [ ] README에 접근법·재현 절차·성능 수치 작성
- [ ] 결과보고서 5쪽 이내 작성 (공식 양식, 첫 쪽 안내문 삭제)
- [ ] AI 모델 항목 — 미탑재 시 `해당 없음 — 실행 이미지에 AI 모델을 탑재하지 않음`
- [ ] AI 코딩 도구 사용 범위 기재
- [ ] SBOM 항목, 데모 영상 URL

### 🔒 8/25 24:00 — 이미지 동결
- [ ] 이후 라우터 코드 변경 금지. 문서·보고서·제출 JSON만 수정

**게이트 P4** → 공식 자원 조건에서 3등급 전부 유효한 `submission.json` 생성, 라이선스 검증 통과.

---

## P5 — 버퍼·제출 (8/26–8/27)

### 8/26 — 제출 절차 (RULES F3 순서 엄수)
- [ ] ① 최종 코드 커밋 push
- [ ] ② **그 커밋에서** `linux/arm64` 이미지 빌드
- [ ] ③ 공개 레지스트리 push, 전체 다이제스트 확보
- [ ] ④ `submission-ossp-skt.json` 작성 → `tools/validate_technical_submission.py` 통과 → **별도 커밋**
- [ ] ⑤ `https://github.com/<계정>/<저장소>/tree/<40자리 SHA>` 스냅샷 URL 확보
- [ ] fork가 로그아웃 상태에서 열리는지 확인

### 8/27 (마감 18:00, 여유 있게 오전에)
- [ ] osscontest.kr에 결과보고서 원본 + PDF 업로드
- [ ] `프로젝트 등록 URL`에 스냅샷 URL 기재
- [ ] 접수 확인 기록 확인

---

## 상시 규칙

- 커밋마다 6개 숫자(3등급 × 점수/비율)를 커밋 메시지에 남긴다
- 예측기·특징·정책을 바꾸면 파산 게이트를 **자동으로** 다시 돌린다
- all-light(0.6193)보다 낮은 구성은 절대 제출하지 않는다 — 폴백 브랜치를 끝까지 유지
- 트랙에 손대기 전에 킬 기준과 마감을 먼저 적는다

## 하지 않기로 한 것

| 항목 | 이유 |
| --- | --- |
| 임베딩 모델(MiniLM급) 탑재 | 구조적 특징 진단에서 "다른 표현이 도움된다"는 근거가 **기각**됨 (DESIGN §6) |
| hashed n-gram의 SVD/랜덤투영 | 같은 표현공간의 선형변환 — 가설을 검증조차 못 함 |
| 모델 순차 호출·답변 비교·승급 | 규칙상 금지 |
| 원본 저장소로 PR | `CONTRIBUTING.md`가 외부 기여를 받지 않음 |
| 라우터 실행 속도 최적화 | 동점 처리용일 뿐. 시간 여유 12배 (DESIGN §2.7) |
